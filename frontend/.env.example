# See ../../LINKS.md for how FRONTEND, BACKEND, and SHAKEUS URLs fit together.
#
# Local dev: leave empty — Vite proxies /api and /health to http://127.0.0.1:8060 (vite.config.ts).
# Production: full URL of your backend (DigitalOcean), no trailing slash.
VITE_API_BASE_URL=https://seahorse-app-74te5.ondigitalocean.app/
#
# GitHub Project Pages subpath only:
# VITE_BASE_PATH=/your-repo-name/
